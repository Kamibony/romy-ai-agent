{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "E2E: Dashboard",
      "request": "launch",
      "type": "dart",
      "program": "${workspaceFolder}/lib/main.dart",
      "cwd": "${workspaceFolder}",
      "args": [
        "-d",
        "web",
        "--web-port",
        "3000"
      ],
      "preLaunchTask": "Root: Setup Dev Environment"
    }
  ]
}