import 'package:flutter/material.dart';

class MemoryManagerScreen extends StatelessWidget {
  const MemoryManagerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Memory Manager - View Firestore Rules'),
    );
  }
}
