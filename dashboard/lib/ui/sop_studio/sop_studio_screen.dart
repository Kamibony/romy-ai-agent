import 'package:flutter/material.dart';

class SopStudioScreen extends StatelessWidget {
  const SopStudioScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('SOP Studio - Write Standard Operating Procedures'),
    );
  }
}
